namespace jade.net.nodes
{
    internal class Mixin : Attributes
    {
        internal Mixin(string name, string args, Block block, bool call)
        {
            Name = name;
            // Args = args;
            Block = block;
            // Call = call;
        }
    }
}